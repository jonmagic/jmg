import React from 'react'
import ReactDOM from 'react-dom/client'
import Keyfall from './keyfall.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Keyfall />
  </React.StrictMode>,
)
